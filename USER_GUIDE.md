# NotebookLM Source Auto-Updater ユーザーガイド

## 目次
1. [概要](#概要)
2. [インストール方法](#インストール方法)
3. [使用方法](#使用方法)
4. [トラブルシューティング](#トラブルシューティング)
5. [よくある質問（FAQ）](#よくある質問faq)
6. [お問い合わせ](#お問い合わせ)

## 概要

NotebookLM Source Auto-Updaterは、Google NotebookLMのソースを効率的に一括更新するためのChrome拡張機能です。

### 主な機能
- **全ソース自動更新**: すべてのソースを一括で自動的に更新
- **指定ソース更新**: フィルタ機能で特定のソースのみを更新
- **検索プレビュー**: 更新前に該当するソースを確認
- **進捗表示**: 更新状況をリアルタイムで表示

## インストール方法

### 手順
1. Chrome Web Storeのリンクにアクセス
   ```
   https://chrome.google.com/webstore/detail/[拡張機能ID]
   ```
   ※ リンクは社内で共有されます

2. 「Chromeに追加」ボタンをクリック

3. 権限の確認ダイアログが表示されます
   - 必要な権限: NotebookLMページへのアクセス
   - 「拡張機能を追加」をクリック

4. インストール完了
   - Chromeのツールバーに拡張機能アイコンが表示されます
   - NotebookLMを開いて使用開始できます

### インストールの確認
1. NotebookLMページ（https://notebooklm.google.com/）を開く
2. Chromeツールバーの拡張機能アイコンをクリック
3. ポップアップが表示されればインストール成功

## 使用方法

### 基本的な使い方

#### 1. 全ソースを一括更新

最もシンプルな使用方法です。NotebookLM内のすべてのソースを一度に更新します。

**手順:**
1. NotebookLMを開く
2. 拡張機能アイコンをクリック
3. モードが「全ソース」になっていることを確認
4. 「全ソースを自動更新」ボタンをクリック
5. 更新が自動的に実行されます

**注意:**
- ソース数が多い場合、完了まで時間がかかります
- 更新中はページを閉じないでください

#### 2. 指定ソースのみ更新

特定のソースのみを更新したい場合に使用します。

**手順:**
1. NotebookLMを開く
2. 拡張機能アイコンをクリック
3. トグルを「指定ソース」に切り替え
4. フィルタ欄にキーワードを入力
   - 例: `report, 2024` （カンマ区切りで複数キーワード指定可能）
5. 「検索」ボタンをクリックして該当ソースを確認
   - 一致したソース数が表示されます
   - プレビュー機能で該当ソースが確認できます
6. 「指定ソースを自動更新」ボタンをクリック
7. 該当するソースのみが更新されます

**フィルタのコツ:**
- カンマ区切りで複数キーワードを指定できます
- 各キーワードは「OR」条件で検索されます
- 大文字・小文字は区別されません
- 部分一致で検索されます

**使用例:**
- `report` → "report"を含むすべてのソース
- `2024, 2025` → "2024"または"2025"を含むソース
- `meeting, 議事録` → "meeting"または"議事録"を含むソース

### 進捗の確認

更新中は以下の情報が表示されます：
- 現在処理中のソース番号
- 全体のソース数
- 進行状況（例: "3 / 10"）

### 更新の完了

更新が完了すると、以下のメッセージが表示されます：
- 「✓ 更新完了！」（全ソース）
- 「✓ 指定ソースの更新完了！」（指定ソース）

## トラブルシューティング

### 拡張機能が動作しない

**症状:** アイコンをクリックしても何も起こらない

**解決方法:**
1. ページをリロードしてください（F5またはCmd+R）
2. 拡張機能が有効になっているか確認
   - `chrome://extensions/`にアクセス
   - 本拡張機能が「オン」になっているか確認
3. Chromeを再起動してみてください

### 更新ボタンが見つからないというエラー

**症状:** 「更新ボタンが見つかりません」とエラーが表示される

**原因:**
- NotebookLMのページ構造が変更された可能性があります
- ソースが存在しない可能性があります

**解決方法:**
1. NotebookLMページに実際に更新可能なソースがあるか確認
2. ページを完全にリロードしてください
3. 問題が続く場合は、サポートにお問い合わせください

### 検索で該当ソースが見つからない

**症状:** フィルタを入力しても「0件一致」と表示される

**解決方法:**
1. キーワードのスペルを確認してください
2. より短いキーワードで試してください（部分一致で検索されます）
3. カンマの前後にスペースが入っていないか確認してください

### 更新が途中で止まる

**症状:** 一部のソースが更新されずに停止する

**解決方法:**
1. ページをリロードして最初からやり直してください
2. インターネット接続を確認してください
3. NotebookLM自体がエラーを出していないか確認してください

### 拡張機能が自動更新されない

**症状:** 新バージョンがリリースされたが更新されない

**解決方法:**
1. Chromeを再起動してください（通常は自動更新されます）
2. 手動で更新を確認:
   - `chrome://extensions/`にアクセス
   - 「デベロッパーモード」をオンにする
   - 「更新」ボタンをクリック

## よくある質問（FAQ）

### Q1: この拡張機能は無料ですか？
**A:** はい、社内利用者向けに無料で提供されています。

### Q2: NotebookLM以外のページでも使えますか？
**A:** いいえ、本拡張機能はNotebookLM（notebooklm.google.com）でのみ動作します。

### Q3: データは安全ですか？
**A:** はい。本拡張機能は一切のデータを収集・送信しません。すべての処理はブラウザ内で完結します。詳細は[PRIVACY_POLICY.md](./PRIVACY_POLICY.md)をご確認ください。

### Q4: 一度に何個のソースを更新できますか？
**A:** 技術的な制限はありませんが、あまりに多い場合は時間がかかることがあります。指定ソース機能を使って分割更新することをおすすめします。

### Q5: 更新の間隔を調整できますか？
**A:** 現在のバージョンでは固定の間隔で更新されます。将来のバージョンで調整可能になる可能性があります。

### Q6: スマートフォンでも使えますか？
**A:** いいえ、Chrome拡張機能はデスクトップ版Chromeでのみ動作します。

### Q7: 他のブラウザ（Edge、Firefox等）でも使えますか？
**A:** 現在はChromeおよびChromiumベースのブラウザ（Microsoft Edge等）でのみ動作します。

### Q8: 更新をキャンセルできますか？
**A:** 現在のバージョンでは、更新開始後のキャンセル機能はありません。将来のバージョンで追加予定です。

### Q9: エラーが発生したらどうすればいいですか？
**A:** まずページをリロードしてみてください。解決しない場合は、[お問い合わせ](#お問い合わせ)までご連絡ください。

### Q10: 新機能のリクエストはできますか？
**A:** はい、ぜひお寄せください。[お問い合わせ](#お問い合わせ)からご連絡ください。

## お問い合わせ

### サポート
本拡張機能に関するご質問、不具合の報告、機能要望などは、以下までお問い合わせください。

**連絡先:**
- メール: shirahama.junya@moneyforward.co.jp

### フィードバック
ご意見やご感想もお待ちしております。より良いツールにするため、皆様のフィードバックを活用させていただきます。

---

**バージョン:** 1.0.0
**最終更新:** 2026年1月28日
